export class Zone {
    id: string;
    serialNo: number;
    name: string;
    nameBd: string;
    note: string;

    constructor(
        id?: string,
        serialNo?: number,
        name?: string,
        nameBd?: string,
        note?: string
    ) { }
}